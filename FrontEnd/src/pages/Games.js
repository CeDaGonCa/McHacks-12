import React from 'react';
import './Page.css'; // Import the CSS file for page styling

const Games = () => {
    return (
        <div className="page-content">
            <h1>Games Page</h1>
            <p>This is the games page of our application.</p>
        </div>
    );
};

export default Games;
